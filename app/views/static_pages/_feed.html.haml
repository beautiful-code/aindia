-# .col-md-
.col-md-8
  - if @feed_items.any?
    .feed_items
      = render(partial: "feed_item", collection: @feed_items, as: 'issue') || "There's no ad to be displayed"
      -# = render partial: "feed_item", as: 'issue', collection: @feed_items
      -# - @feed_items.each do |feed_item|   || "Theres no feed to display. Be the first to create one"
      -# = render @feed_items
  -# = will_paginate @feed_items
.col-md-3
